[workspace]
members = [
    "client-core",
    "client-daemon",
    "common-core",
    "proxy-core",
    "proxy-daemon",
    "tinc-plugin",
]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
incremental = false
opt-level = "z"
