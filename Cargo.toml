[workspace]
members = [
    "client-daemon",
    "common-core",
    "proxy-daemon",
    "tinc-plugin",
]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
incremental = false
opt-level = "z"
